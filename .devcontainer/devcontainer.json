{
    "name": "ltw remote test",
    "dockerComposeFile" : ["../docker-compose.yml"], 
    "service": "web",
    "extensions": ["oderwat.indent-rainbow"],
    "workspaceFolder": "/app",
    "postCreateCommand": "apt update && apt install -y git" 

}